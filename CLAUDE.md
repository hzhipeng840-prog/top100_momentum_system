# AI 编码规则

这份文件是本仓库给 AI 编码助手用的可编辑规则文件。

目标只有两个：

1. 降低 AI 改代码时的错误率
2. 保护这套 Top100 情绪动量系统的数据语义和运行链路

---

## 一. 通用 AI 编码规则

### 1. 先想清楚，再动代码
- 先确认这次修改影响的是：
  - 页面展示
  - 数据口径
  - 自动化流程
  - 或策略逻辑
- 如果一个改动会同时影响多层，先拆开处理，不要混改。

### 2. 优先最小正确修改
- 先做最小可验证修复，不要顺手重构无关部分。
- 没有证据证明更复杂的改法更好时，优先简单方案。

### 3. 只改必要范围
- UI 问题尽量只改 `app.py`
- 规则问题尽量只改 `src/signals.py`
- 流程问题尽量只改 `src/pipeline.py` / workflow
- 不要为了改一个 bug 顺手大面积改列名、文件名、输出格式

### 4. 先定义成功标准，再验证
- 每次修改前，先想清楚这次“修好”的标准是什么
- 修改后至少验证：
  - 语法是否通过
  - 相关测试是否通过
  - 关键数据文件是否真的变成预期状态

### 5. 不要把“解释”当成“证据”
- 页面显示正常，不等于数据链路正常
- 需要直接检查：
  - `data/processed/*.csv`
  - `data/reports/*.csv`
  - workflow run 状态
  - 最新日期 / 已结算数量 / freshness

---

## 二. 本项目专属规则

### 1. 先分清三类流程

#### `morning_capture`
- 早盘轻量流程
- 主要服务 `v2`
- 只更新当天轻量结果
- 不应被当成正式收盘结果

#### `tail_capture`
- 尾盘轻量流程
- 主要服务 `v3`
- 只更新当天轻量结果
- 不应被当成正式收盘结果

#### `full / post_close`
- 正式收盘流程
- 负责正式结算和正式回写
- freshness 的判断以它为准

### 2. `recompute` 只用于本地重算
- `recompute` 不抓新数据
- 它只基于当前缓存重算
- 不能把 `recompute` 结果当成“当天正式结果”
- 不要把 `recompute` 产物推成云端正式主结果

### 3. 优化执行时间时，不能牺牲结算完整性
- 轻量流程可以跳过：
  - `strong_recap`
  - `backtest`
  - `lesson_evaluation`
  - 全量 `followups`
- 但 `full` 不能因为提速而漏掉：
  - 当天 `post_close` 100 只样本的价格刷新
  - `v1/v2/v3` 的正式结算
  - `market_regime` 最新性

### 4. 任何改动如果影响 `full`，必须验证 freshness
至少检查：
- `latest_push*.csv` 最新日期是否正确
- `followups*.csv` 对应日期 `settled_1d` 是否合理
- `market_regime.csv` 是否滞后
- 页面黄条是否符合真实数据状态

### 5. 先看数据，再判断是不是页面 bug
如果页面显示不对，优先核：
- `latest_push.csv`
- `fast_strategy.csv`
- `signals*.csv`
- `followups*.csv`
- `market_regime.csv`

页面旧状态常见原因：
- Streamlit 进程没刷新
- 云端结果没真正回写
- 本地同步到的是旧提交

### 6. `v1 / v2 / v3 / v4` 的职责不要混

#### `v1`
- 收盘版
- 稳定基线
- 不要轻易因为实验规则改坏它

#### `v2`
- 早盘版
- 重点看次日可执行性

#### `v3`
- 尾盘版
- 重点看尾盘承接与可买性

#### `v4`
- 试运行影子版
- 允许继续积累
- 不默认进入主版本选择器
- 没有显著优势前，不升级为正式主版本

### 7. “可买性闸门”要和“推送层级”分开理解
- 很强的票不等于当天可买
- 一字板 / 封板票可以进入观察层
- 但默认执行单不应把明显买不进去的票塞给用户

### 8. 自动化失败时，先分层定位
排查顺序：
1. `cron-job.org` 是否真的触发
2. GitHub 是否接受 `workflow_dispatch`
3. Actions run 是否成功
4. 仓库是否真的回写了新数据
5. 本地是否同步到了新提交

不要一上来就假设“页面坏了”。

### 9. 不要轻易 force push `main`
- 只有在用户明确同意时，才允许 `--force-with-lease`
- 因为这会覆盖云端自动化已经写回的数据历史

### 10. 任何本地实验不要默认污染正式版本
- `v4`、规则试验、UI 试验，默认先留本地
- 没有明确效果证据，不要直接推上云端正式主线

---

## 三. 每次改动后的最小检查清单

### 页面改动
- `python -m py_compile app.py`
- 页面是否还读得到最新 CSV
- 是否引入重复字段 / 错位字段 / 缓存旧状态

### 策略改动
- 相关测试通过
- 至少对比：
  - 次日收盘
  - 3日
  - 5日
- 不只看均值，也看胜率和样本数

### 流程改动
- 检查轻量流程是否仍然轻量
- 检查 `full` 是否仍能完整结算
- 检查云端回写是否正常

### 数据异常
- 先查磁盘文件，再查页面
- 先查是否是旧缓存，再查是否是代码问题

---

## 四. 当前推荐原则

- `v1` 保持稳定
- `v2/v3` 小步优化
- `v4` 继续积累，不急着升级
- `full` 的正确性优先于运行速度
- 页面解释可以简化，但数据语义不能糊

---

## 五. 这份文件怎么用

以后如果要调整规则，优先改这里：
- 新增约束
- 删除不再适用的流程规则
- 补充本项目已经踩过的坑

原则是：
- 写短
- 写硬
- 写能执行的规则
- 不写空话
