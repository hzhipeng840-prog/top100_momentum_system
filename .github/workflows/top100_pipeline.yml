name: Top100 Momentum Pipeline

on:
  push:
    branches:
      - main
    paths-ignore:
      - data/raw/popularity_top100.csv
      - data/raw/intraday_snapshots.csv
      - data/processed/daily_features.csv
      - data/processed/signals*.csv
      - data/processed/followups*.csv
      - data/processed/fast_strategy_history*.csv
      - data/processed/market_regime.csv
      - data/reports/*.csv
  workflow_dispatch:
    inputs:
      mode:
        description: Run mode
        required: true
        default: full
        type: choice
        options:
          - full
          - tail_capture
          - recompute
          - backtest
          - tests
      force_refresh_prices:
        description: Force remote price refresh
        required: false
        default: false
        type: boolean
  schedule:
    - cron: "57 7 * * *"
    - cron: "30-55/5 6 * * 1-5"
    - cron: "10-30/5 9 * * 1-5"

concurrency:
  group: top100-momentum-pipeline
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write
    env:
      PYTHONUTF8: "1"
      TZ: Asia/Shanghai
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Resolve mode
        id: resolve
        shell: bash
        run: |
          mode="${{ inputs.mode }}"
          force_refresh="${{ inputs.force_refresh_prices }}"
          sync_back="false"

          if [ "${{ github.event_name }}" = "push" ]; then
            mode="tests"
            force_refresh="false"
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            case "${{ github.event.schedule }}" in
              "57 7 * * *")
                mode="tests"
                ;;
              "30-55/5 6 * * 1-5")
                mode="tail_capture"
                ;;
              "10-30/5 9 * * 1-5")
                mode="full"
                ;;
              *)
                mode="full"
                ;;
            esac
            force_refresh="false"
            sync_back="true"
          elif [ "${mode}" != "tests" ]; then
            sync_back="true"
          fi

          echo "mode=${mode}" >> "$GITHUB_OUTPUT"
          echo "force_refresh=${force_refresh}" >> "$GITHUB_OUTPUT"
          echo "sync_back=${sync_back}" >> "$GITHUB_OUTPUT"

      - name: Gate scheduled run to expected capture window
        id: capture_window
        shell: bash
        run: |
          should_run="true"
          skip_reason=""
          if [ "${{ github.event_name }}" = "schedule" ]; then
            mode="${{ steps.resolve.outputs.mode }}"
            if [ "$mode" = "tests" ]; then
              should_run="true"
            elif [ "$mode" = "tail_capture" ]; then
              capture_type="intraday_1430"
            else
              capture_type="post_close"
            fi

            if [ "$mode" != "tests" ] && ! python -c "from src.trading_calendar import is_within_capture_window; import sys; sys.exit(0 if is_within_capture_window('${capture_type}') else 1)"; then
              should_run="false"
              skip_reason="outside_expected_capture_window"
            fi
          fi

          echo "should_run=${should_run}" >> "$GITHUB_OUTPUT"
          echo "skip_reason=${skip_reason}" >> "$GITHUB_OUTPUT"

      - name: Create artifact directory
        run: mkdir -p workflow_artifacts/logs

      - name: Write skipped schedule summary
        if: steps.capture_window.outputs.should_run != 'true'
        shell: bash
        run: |
          cat > workflow_artifacts/workflow_summary.md <<EOF
          # Top100 Workflow Summary

          - Status: \`skipped\`
          - Mode: \`${{ steps.resolve.outputs.mode }}\`
          - Reason: \`${{ steps.capture_window.outputs.skip_reason }}\`
          - Current Time (Asia/Shanghai): \`$(TZ=Asia/Shanghai date '+%Y-%m-%dT%H:%M:%S%z')\`
          EOF

      - name: Run selected task
        if: steps.capture_window.outputs.should_run == 'true'
        shell: bash
        run: |
          cmd=(python workflow_job.py --mode "${{ steps.resolve.outputs.mode }}" --summary-path workflow_artifacts/workflow_summary.md --log-dir workflow_artifacts/logs)
          if [ "${{ steps.resolve.outputs.force_refresh }}" = "true" ]; then
            cmd+=(--force-refresh-prices)
          fi
          "${cmd[@]}"

      - name: Commit generated outputs back to repository
        if: steps.resolve.outputs.sync_back == 'true' && steps.capture_window.outputs.should_run == 'true'
        shell: bash
        run: |
          shopt -s nullglob
          files=(
            data/raw/popularity_top100.csv
            data/raw/intraday_snapshots.csv
            data/processed/daily_features.csv
            data/processed/signals*.csv
            data/processed/followups*.csv
            data/processed/fast_strategy_history*.csv
            data/processed/market_regime.csv
            data/reports/*.csv
          )

          if [ ${#files[@]} -eq 0 ]; then
            echo "No generated outputs found to sync."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f "${files[@]}"

          if git diff --cached --quiet; then
            echo "No report changes to commit."
            exit 0
          fi

          git commit -m "Sync pipeline outputs (${{ steps.resolve.outputs.mode }})"
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: top100-${{ steps.resolve.outputs.mode }}-${{ github.run_id }}
          retention-days: 14
          path: |
            workflow_artifacts/
            data/reports/
            data/processed/daily_features.csv
            data/processed/signals*.csv
            data/processed/followups*.csv
            data/processed/fast_strategy_history*.csv
            data/processed/market_regime.csv
            data/raw/popularity_top100.csv
            data/raw/intraday_snapshots.csv
